name: helloworld_workflow
on:
  push:
      branches:
      - main
  pull_request:
      branches:
      - main
  workflow_dispatch:
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: hello_world
        run: echo "helloworld"
        shell: bash

  goodbye:
     runs-on: ubuntu-latest
     steps:
       - name: goodbye_world_2
         run: echo "goodbyeworld"
         shell: bash
    
      
   
  
  


  
  
  

  
