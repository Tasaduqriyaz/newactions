name: helloworld_workflow
on:
  push:
      branches:
      - main
  pull_request:
      branches:
      - main
  workflow_dispatch:
jobs:
  hello:
    runs_on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: hello_world
        run: echo "helloworld"
        shell: bash

  goodbye:
     runs_on: ubuntu-latest
     steps:
       - name: goodbye_world
         run: echo "goodbyeworld"
         shell: bash
    
      
   
  
  


  
  
  

  
